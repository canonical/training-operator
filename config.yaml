options:
  training-operator-image:
    default: charmedkubeflow/training-operator:1.8.0-ddaf82a
    description: |
      Container image to be used by the training-operator workload.
    type: string
